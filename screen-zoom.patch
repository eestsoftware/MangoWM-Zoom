From 8259538d0013d21c0b8ca1f9590562b7696f33da Mon Sep 17 00:00:00 2001
From: Silenceman <eestsoftware@gmail.com>
Date: Tue, 22 Sep 2026 22:58:54 +0300
Subject: [PATCH] Better multi monitor zoom implementation

---
 include/mango/common/server.h       |   7 ++
 include/mango/config/parse_config.h |   3 +
 include/mango/dispatch/bind.h       |   4 +
 include/mango/manage/client.h       |   2 +
 src/common/server.c                 |   6 ++
 src/config/parse_config.c           |  17 ++++
 src/dispatch/bind.c                 |  42 ++++++++
 src/main.c                          |   4 +
 src/manage/client.c                 | 149 ++++++++++++++++++++++++++++
 src/manage/monitor.c                |   6 +-
 10 files changed, 239 insertions(+), 1 deletion(-)

diff --git a/include/mango/common/server.h b/include/mango/common/server.h
index 3949a4f0..d7bbb5fe 100644
--- a/include/mango/common/server.h
+++ b/include/mango/common/server.h
@@ -234,6 +234,13 @@ struct MangoServer {
 	struct wl_listener new_foreign_toplevel_capture_listener;
 	struct wlr_ext_foreign_toplevel_list_v1 *foreign_toplevel_list;
 
+	/*        Zoooooom!       */
+
+	struct wlr_swapchain *zoom_swapchain;
+	float zoom_level;
+	float zoom_target;
+	int32_t zoom_animating;
+
 #ifdef XWAYLAND
 	struct wlr_xwayland *xwayland;
 	struct wl_event_source *sync_keymap;
diff --git a/include/mango/config/parse_config.h b/include/mango/config/parse_config.h
index e28a6d09..b2bf0b5a 100644
--- a/include/mango/config/parse_config.h
+++ b/include/mango/config/parse_config.h
@@ -457,6 +457,9 @@ typedef struct {
 	int32_t shadows_position_x;
 	int32_t shadows_position_y;
 	float shadowscolor[4];
+	/* screen zoom */
+	float zoom_max;
+	float zoom_speed;
 	int32_t dim_enable;
 	float dim_focused_color[4];
 	float dim_unfocused_color[4];
diff --git a/include/mango/dispatch/bind.h b/include/mango/dispatch/bind.h
index e76405be..f4bffa26 100644
--- a/include/mango/dispatch/bind.h
+++ b/include/mango/dispatch/bind.h
@@ -118,6 +118,10 @@ int32_t sleep_monitor(const Arg *arg);
 int32_t wakeup_monitor(const Arg *arg);
 int32_t sleep_toggle_monitor(const Arg *arg);
 int32_t scroller_stack(const Arg *arg);
+int32_t screen_zoom_in(const Arg *arg);
+int32_t screen_zoom_out(const Arg *arg);
+int32_t screen_zoom_reset(const Arg *arg);
+int32_t screen_zoom_set(const Arg *arg);
 int32_t toggle_all_floating(const Arg *arg);
 int32_t dwindle_toggle_split_direction(const Arg *arg);
 int32_t dwindle_split_horizontal(const Arg *arg);
diff --git a/include/mango/manage/client.h b/include/mango/manage/client.h
index e289e945..aea0361f 100644
--- a/include/mango/manage/client.h
+++ b/include/mango/manage/client.h
@@ -387,6 +387,8 @@ void set_minimized(Client *c);
 void unminimize(Client *c);
 void exit_scroller_stack(Client *c);
 void clear_fullscreen_and_maximized_state(Monitor *m);
+void screen_zoom_update();
+void render_zoomed(Monitor *m);
 
 /* Clears the fullscreen flag and restores the border zeroed at fullscreen. */
 void clear_fullscreen_flag(Client *c);
diff --git a/src/common/server.c b/src/common/server.c
index 838c428b..c7addae3 100644
--- a/src/common/server.c
+++ b/src/common/server.c
@@ -81,6 +81,12 @@ struct MangoServer server = {
 	.last_cursor_surface_destroy_listener =
 		{.notify = handle_last_cursor_surface_destroy},
 
+	.zoom_swapchain = NULL,
+	.zoom_level = 1.0f,
+	.zoom_target = 1.0f,
+	.zoom_animating = 0,
+	
+
 #ifdef XWAYLAND
 	.new_xwayland_surface_listener = {.notify = handle_new_xwayland_surface},
 	.xwayland_ready_listener = {.notify = handle_xwayland_ready},
diff --git a/src/config/parse_config.c b/src/config/parse_config.c
index 60050075..b8c36acd 100644
--- a/src/config/parse_config.c
+++ b/src/config/parse_config.c
@@ -455,6 +455,10 @@ bool parse_option(Config *config, char *key, char *value, int line_number) {
 		config->zoom_initial_ratio = atof(value);
 	} else if (strcmp(key, "zoom_end_ratio") == 0) {
 		config->zoom_end_ratio = atof(value);
+	} else if (strcmp(key, "zoom_max") == 0) {
+		config->zoom_max = atof(value);
+	} else if (strcmp(key, "zoom_speed") == 0) {
+		config->zoom_speed = atof(value);
 	} else if (strcmp(key, "fadein_begin_opacity") == 0) {
 		config->fadein_begin_opacity = atof(value);
 	} else if (strcmp(key, "fadeout_begin_opacity") == 0) {
@@ -3774,6 +3778,8 @@ void override_config(void) {
 	config.zoom_initial_ratio =
 		CLAMP_FLOAT(config.zoom_initial_ratio, 0.1f, 1.0f);
 	config.zoom_end_ratio = CLAMP_FLOAT(config.zoom_end_ratio, 0.1f, 1.0f);
+	config.zoom_max = CLAMP_FLOAT(config.zoom_max, 1.0f, 20.0f);
+	config.zoom_speed = CLAMP_FLOAT(config.zoom_speed, 0.01f, 5.0f);
 	config.fadein_begin_opacity =
 		CLAMP_FLOAT(config.fadein_begin_opacity, 0.0f, 1.0f);
 	config.fadeout_begin_opacity =
@@ -4019,6 +4025,8 @@ void set_value_default() {
 	config.tag_animation_direction = HORIZONTAL;
 	config.zoom_initial_ratio = 0.4f;
 	config.zoom_end_ratio = 0.8f;
+	config.zoom_max = 6.0;
+	config.zoom_speed = 0.3;
 	config.fadein_begin_opacity = 0.5f;
 	config.fadeout_begin_opacity = 0.5f;
 	config.animation_duration_move = 500;
@@ -4801,6 +4809,15 @@ FuncType parse_func_name(char *func_name, Arg *arg, char *arg_value,
 	} else if (strcmp(func_name, "focusdir") == 0) {
 		func = focus_direction;
 		(*arg).i = parse_direction(arg_value);
+	} else if (strcmp(func_name, "screen_zoom_in") == 0) {
+		func = screen_zoom_in;
+	} else if (strcmp(func_name, "screen_zoom_out") == 0) {
+		func = screen_zoom_out;
+	} else if (strcmp(func_name, "screen_zoom_reset") == 0) {
+		func = screen_zoom_reset;
+	} else if (strcmp(func_name, "screen_zoom_set") == 0) {
+		func = screen_zoom_set;
+		(*arg).f = atof(arg_value);
 	} else if (strcmp(func_name, "focus_window_or_workspace") == 0) {
 		func = focus_window_or_workspace;
 		(*arg).i = parse_direction(arg_value);
diff --git a/src/dispatch/bind.c b/src/dispatch/bind.c
index 02e5654f..be004a93 100644
--- a/src/dispatch/bind.c
+++ b/src/dispatch/bind.c
@@ -2688,6 +2688,48 @@ int32_t dwindle_toggle_current_split(const Arg *arg) {
 	return 0;
 }
 
+/* Screen zoom functions */
+
+static void screen_zoom_start_animation(void) {
+	server.zoom_animating = 1;
+}
+
+int32_t screen_zoom_in(const Arg *arg) {
+	server.zoom_target += config.zoom_speed;
+	if (server.zoom_target > config.zoom_max)
+		server.zoom_target = config.zoom_max;
+	screen_zoom_start_animation();
+	request_fresh_all_monitors();
+	return 0;
+}
+
+int32_t screen_zoom_out(const Arg *arg) {
+	server.zoom_target -= config.zoom_speed;
+	if (server.zoom_target < 1.0f)
+		server.zoom_target = 1.0f;
+	screen_zoom_start_animation();
+	request_fresh_all_monitors();
+	return 0;
+}
+
+int32_t screen_zoom_reset(const Arg *arg) {
+	server.zoom_target = 1.0f;
+	screen_zoom_start_animation();
+	request_fresh_all_monitors();
+	return 0;
+}
+
+int32_t screen_zoom_set(const Arg *arg) {
+	server.zoom_target = arg->f;
+	if (server.zoom_target < 1.0f)
+		server.zoom_target = 1.0f;
+	if (server.zoom_target > config.zoom_max)
+		server.zoom_target = config.zoom_max;
+	screen_zoom_start_animation();
+	request_fresh_all_monitors();
+	return 0;
+    }
+
 int32_t focus_by_id(const Arg *arg) {
 	if (!server.selected_monitor || !arg->tc)
 		return 0;
diff --git a/src/main.c b/src/main.c
index 5bd4bca3..9afcb923 100644
--- a/src/main.c
+++ b/src/main.c
@@ -52,6 +52,10 @@
 #include <wlr/backend/wayland.h>
 #include <wlr/interfaces/wlr_keyboard.h>
 #include <wlr/render/allocator.h>
+#include <wlr/render/drm_format_set.h>
+#include <wlr/render/wlr_texture.h>
+#include <wlr/render/pass.h>
+#include <wlr/render/swapchain.h>
 #include <wlr/render/wlr_renderer.h>
 #include <wlr/types/wlr_alpha_modifier_v1.h>
 #include <wlr/types/wlr_color_management_v1.h>
diff --git a/src/manage/client.c b/src/manage/client.c
index 258ac2c3..b17e05a6 100644
--- a/src/manage/client.c
+++ b/src/manage/client.c
@@ -37,6 +37,7 @@
 #include <wlr/types/wlr_linux_drm_syncobj_v1.h>
 #include <wlr/types/wlr_pointer_constraints_v1.h>
 #include <wlr/types/wlr_presentation_time.h>
+#include <wlr/render/swapchain.h>
 #include <wlr/types/wlr_seat.h>
 #include <wlr/types/wlr_subcompositor.h>
 #include <wlr/types/wlr_xcursor_manager.h>
@@ -2604,6 +2605,154 @@ void client_set_opacity(Client *c, double opacity) {
 								   scene_buffer_apply_opacity, &opacity);
 }
 
+void screen_zoom_update() {
+	if (!server.zoom_animating)
+		return;
+
+	float diff = server.zoom_target - server.zoom_level;
+	if (fabsf(diff) < 0.001f) {
+		server.zoom_level = server.zoom_target;
+		server.zoom_animating = 0;
+		if (server.zoom_level <= 1.0f) {
+			server.zoom_level = 1.0f;
+			/* Free zoom swapchain when not zoomed */
+			if (server.zoom_swapchain) {
+				wlr_swapchain_destroy(server.zoom_swapchain);
+				server.zoom_swapchain = NULL;
+			}
+		}
+		return;
+	}
+
+	/* Smooth ease-out interpolation */
+	server.zoom_level += diff * 0.15f;
+}
+
+void render_zoomed(Monitor *m) {
+	struct wlr_output *output = m->wlr_output;
+	int32_t width = output->width;
+	int32_t height = output->height;
+
+	/* Build normal scene state */
+	struct wlr_output_state state = {0};
+	wlr_output_state_init(&state);
+	if (!wlr_scene_output_build_state(m->scene_output, &state, NULL)) {
+		wlr_log(WLR_ERROR, "[%s:%d] Failed to build zoom scene state",
+				__FILE__, __LINE__);
+		wlr_output_state_finish(&state);
+		return;
+	}
+
+	/* Ensure zoom swapchain exists and matches output size */
+	if (!server.zoom_swapchain || server.zoom_swapchain->width != width ||
+		server.zoom_swapchain->height != height) {
+		if (server.zoom_swapchain)
+			wlr_swapchain_destroy(server.zoom_swapchain);
+
+		const struct wlr_drm_format_set *formats =
+			wlr_renderer_get_texture_formats(server.renderer, server.renderer->render_buffer_caps);
+		const struct wlr_drm_format *fmt =
+			wlr_drm_format_set_get(formats, output->render_format);
+		if (!fmt) {
+			wlr_log(WLR_ERROR, "[%s:%d] Failed to create zoom swapchain",
+					__FILE__, __LINE__);
+			wlr_output_commit_state(output, &state);
+			wlr_output_state_finish(&state);
+			return;
+		}
+		server.zoom_swapchain = wlr_swapchain_create(server.allocator, width, height, fmt);
+		if (!server.zoom_swapchain) {
+			wlr_log(WLR_ERROR, "[%s:%d] Failed to create zoom swapchain",
+					__FILE__, __LINE__);
+			wlr_output_commit_state(output, &state);
+			wlr_output_state_finish(&state);
+			return;
+		}
+	}
+
+	/* Acquire zoom buffer */
+	struct wlr_buffer *zoom_buf = wlr_swapchain_acquire(server.zoom_swapchain);
+	if (!zoom_buf) {
+		wlr_output_commit_state(output, &state);
+		wlr_output_state_finish(&state);
+		return;
+	}
+
+	/* Create texture from the scene buffer */
+	struct wlr_texture *scene_tex =
+		wlr_texture_from_buffer(server.renderer, state.buffer);
+	if (!scene_tex) {
+		wlr_log(WLR_ERROR, "[%s:%d] Failed to create zoom texture",
+				__FILE__, __LINE__);
+		wlr_buffer_unlock(zoom_buf);
+		wlr_output_commit_state(output, &state);
+		wlr_output_state_finish(&state);
+		return;
+	}
+
+	/* Begin render pass on zoom buffer */
+	struct wlr_render_pass *pass =
+		wlr_renderer_begin_buffer_pass(server.renderer, zoom_buf, NULL);
+	if (!pass) {
+		wlr_log(WLR_ERROR, "[%s:%d] Failed to begin zoom render pass",
+				__FILE__, __LINE__);
+		wlr_texture_destroy(scene_tex);
+		wlr_buffer_unlock(zoom_buf);
+		wlr_output_commit_state(output, &state);
+		wlr_output_state_finish(&state);
+		return;
+	}
+
+	/* Calculate viewport centered on cursor */
+	double cx = (server.cursor->x - m->m.x) * output->scale;
+	double cy = (server.cursor->y - m->m.y) * output->scale;
+
+	double vw = (double)width / server.zoom_level;
+	double vh = (double)height / server.zoom_level;
+
+	double vx = cx - vw / 2.0;
+	double vy = cy - vh / 2.0;
+
+	/* Clamp viewport to buffer bounds */
+	if (vx < 0)
+		vx = 0;
+	if (vy < 0)
+		vy = 0;
+	if (vx + vw > width)
+		vx = width - vw;
+	if (vy + vh > height)
+		vy = height - vh;
+
+	/* Clear and draw zoomed scene */
+	wlr_render_pass_add_rect(pass,
+		&(struct wlr_render_rect_options){
+			.box = {0, 0, width, height},
+			.color = {0, 0, 0, 1},
+			.blend_mode = WLR_RENDER_BLEND_MODE_NONE,
+		});
+
+	wlr_render_pass_add_texture(pass,
+		&(struct wlr_render_texture_options){
+			.texture = scene_tex,
+			.src_box = {vx, vy, vw, vh},
+			.dst_box = {0, 0, width, height},
+			.filter_mode = WLR_SCALE_FILTER_BILINEAR,
+			.blend_mode = WLR_RENDER_BLEND_MODE_NONE,
+		});
+
+	wlr_render_pass_submit(pass);
+	wlr_texture_destroy(scene_tex);
+
+	/* Replace buffer in output state with zoomed buffer */
+	wlr_output_state_set_buffer(&state, zoom_buf);
+	wlr_buffer_unlock(zoom_buf);
+
+	/* Commit the zoomed state */
+	wlr_output_commit_state(output, &state);
+	wlr_output_state_finish(&state);
+}
+
+
 void client_ensure_constraint(Client *c) {
 	if (!c || !client_surface(c)) {
 		return;
diff --git a/src/manage/monitor.c b/src/manage/monitor.c
index 80d6aa52..081ddbcd 100644
--- a/src/manage/monitor.c
+++ b/src/manage/monitor.c
@@ -1361,6 +1361,10 @@ void handle_output_frame(struct wl_listener *listener, void *data) {
 		monitor_stop_skip_frame_timer(m);
 	}
 
+	screen_zoom_update();
+
+	if (server.zoom_level > 1.0f && server.selected_monitor == m) render_zoomed(m);
+
 	mango_scene_output_commit(m->scene_output, &m->pending);
 
 skip:
@@ -1369,7 +1373,7 @@ skip:
 	wlr_scene_output_send_frame_done(m->scene_output, &now);
 
 	// If more frames are needed, ensure the next frame is scheduled.
-	if (need_more_frames && server.allow_frame_scheduling) {
+	if ( (need_more_frames || server.zoom_animating) && server.allow_frame_scheduling) {
 		request_fresh_all_monitors();
 	}
 }
-- 
2.55.0

